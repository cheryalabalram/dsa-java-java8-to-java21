package com.balram.demo.actions.SG.main;

public class S_Object {
    //These are the most commonly used methods from the Object class:
    //equals(): Compares objects for equality.
    //hashCode(): Returns the object's hash code.
    //toString(): Provides a string representation of the object.
    //getClass(): Returns the Class object associated with the object.
    //clone(): Creates a copy of the object (requires implementing Cloneable).
    //notify()/notifyAll(): Used for thread communication.
    //wait(): Causes the current thread to wait.
    //finalize(): Called before the object is garbage collected (deprecated in later versions).
}
